# LINQ 기본

오늘 LINQ를 공부했다.

## Where

조건에 맞는 값을 필터링한다.

```csharp
var result = numbers.Where(x => x > 10);
```

## Select

각 요소를 새로운 형태로 변환한다.

```csharp
var result = numbers.Select(x => x * 2);
```

## 기억할 것

- LINQ는 지연 실행되는 경우가 있다.
- IEnumerable과 IQueryable의 차이를 구분해야 한다.
